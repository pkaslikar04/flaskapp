/var/log/flask/*.log {
    daily
    missingok
    rotate 7
    compress
    delaycompress
    notifempty
    sharedscripts
    postrotate
        [ ! -f /var/run/flask.pid ] || kill -USR1 `cat /var/run/flask.pid`
    endscript
}
